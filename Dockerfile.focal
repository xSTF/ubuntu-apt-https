FROM ubuntu:focal

RUN apt-get update &&\
    apt-get install -y\
        apt-transport-https\
        ca-certificates &&\
    apt-get clean && \
    rm -rf \
        /tmp/* \
        /var/tmp/*

COPY focal.list /etc/apt/sources.list
